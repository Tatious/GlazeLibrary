# ============================================================================
# GlazeLibrary - root .gitignore
# ============================================================================

# OS / editor
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Node
node_modules/
**/node_modules/
dist/
**/dist/
dist-ssr/
*.local
*.tsbuildinfo

# ---------------------------------------------------------------------------
# Secrets / environment — NEVER commit
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example
**/firebase-service-account.json
*.pem
*.key

# Deployment artifacts
**/deploy_pkg/
**/deploy.zip
vendor/tatious-ui-*.tgz

# Git history backups (never commit a nested git backup dir)
.git.backup-*
*.git.backup*

# SQLite databases (runtime state; lives on Azure /home in prod)
*.db
*.db-shm
*.db-wal
server/data/

# Test output
playwright-report/
test-results/

# Python
__pycache__/
**/__pycache__/
*.pyc

# ---------------------------------------------------------------------------
# Bulk media & scraper caches
# These are served from Azure Blob Storage / CDN in production and are
# regenerable via the scrapers in scripts/scrapers. Excluded to keep the repo
# small and clonable. See scripts/scrapers/README.md to rebuild.
# ---------------------------------------------------------------------------
public/images/
scripts/scrapers/**/images/
scripts/scrapers/**/html_cache/
scripts/scrapers/**/html_cache_valid/
scripts/scrapers/ai-enrichment/cache/

# Runtime user uploads (stored in Azure Blob in prod)
public/uploads/
